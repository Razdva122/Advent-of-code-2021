export type TTicketElement = number | null;

export type TTicketLine = [TTicketElement, TTicketElement, TTicketElement, TTicketElement, TTicketElement];

export type TTicket = [TTicketLine, TTicketLine, TTicketLine, TTicketLine, TTicketLine];